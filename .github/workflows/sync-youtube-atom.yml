name: YouTube Atom定期同期

on:
  schedule:
    - cron: '23 */6 * * *'
  workflow_dispatch:

permissions:
  actions: write
  contents: write
  pull-requests: write

concurrency:
  group: youtube-atom-sync
  cancel-in-progress: false

jobs:
  sync:
    name: YouTube Atomを同期
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      SYNC_BRANCH: automation/youtube-atom-sync

    steps:
      - name: Check out main
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: main

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Fetch the official YouTube Atom feed
        run: npm run sync:youtube

      - name: Detect snapshot changes
        id: diff
        shell: bash
        run: |
          if git diff --quiet -- src/data/external/youtube-videos.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push the snapshot branch
        if: steps.diff.outputs.changed == 'true'
        id: commit
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/data/external/youtube-videos.json
          git commit -m "[CF-Pages-Skip] YouTube Atomを同期"

          sync_sha="$(git rev-parse HEAD)"
          remote_ref="refs/remotes/origin/${SYNC_BRANCH}"
          git fetch origin \
            "+refs/heads/${SYNC_BRANCH}:${remote_ref}" || true

          if git show-ref --verify --quiet "$remote_ref"; then
            remote_sha="$(git rev-parse "$remote_ref")"
            git push \
              --force-with-lease="refs/heads/${SYNC_BRANCH}:${remote_sha}" \
              origin \
              "HEAD:refs/heads/${SYNC_BRANCH}"
          else
            git push origin "HEAD:refs/heads/${SYNC_BRANCH}"
          fi

          echo "sha=${sync_sha}" >> "$GITHUB_OUTPUT"

      - name: Create or update the sync pull request
        if: steps.diff.outputs.changed == 'true'
        id: pull_request
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail

          pr_number="$(
            gh pr list \
              --repo "$GITHUB_REPOSITORY" \
              --base main \
              --head "$SYNC_BRANCH" \
              --state open \
              --json number \
              --jq '.[0].number // empty'
          )"

          if [[ -z "$pr_number" ]]; then
            pr_body="$(printf '%s\n' \
              '関連 Issue: なし' \
              '' \
              '## 概要' \
              '' \
              '- YouTube公式Atomフィードの静的スナップショットを更新します。' \
              '- 6時間ごとの定期同期で、変更時だけPRを作成します。' \
              '' \
              '## 確認' \
              '' \
              '- Build and Formatを明示起動し、合格後に自動マージします。' \
              '' \
              '## 補足' \
              '' \
              '- 取得時刻だけの変化ではPRを作成しません。' \
              '- ページ閲覧時にYouTube Atomへ再取得しません。' \
              '- 同期branchのPagesプレビューを省き、mainへのマージ後だけ本番ビルドします。')"

            pr_url="$(
              gh pr create \
                --repo "$GITHUB_REPOSITORY" \
                --base main \
                --head "$SYNC_BRANCH" \
                --title "YouTube Atomを定期同期" \
                --body "$pr_body"
            )"
            pr_number="$(
              gh pr view "$pr_url" \
                --repo "$GITHUB_REPOSITORY" \
                --json number \
                --jq '.number'
            )"
          fi

          echo "number=${pr_number}" >> "$GITHUB_OUTPUT"

      - name: Run and wait for the required CI check
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          SYNC_SHA: ${{ steps.commit.outputs.sha }}
        shell: bash
        run: |
          set -euo pipefail

          gh workflow run ci.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref "$SYNC_BRANCH"

          ci_run_id=""
          for _ in $(seq 1 30); do
            ci_run_id="$(
              gh run list \
                --repo "$GITHUB_REPOSITORY" \
                --workflow ci.yml \
                --branch "$SYNC_BRANCH" \
                --commit "$SYNC_SHA" \
                --event workflow_dispatch \
                --limit 1 \
                --json databaseId \
                --jq '.[0].databaseId // empty'
            )"
            [[ -n "$ci_run_id" ]] && break
            sleep 2
          done

          if [[ -z "$ci_run_id" ]]; then
            echo "Dispatched CI run was not found." >&2
            exit 1
          fi

          gh run watch "$ci_run_id" \
            --repo "$GITHUB_REPOSITORY" \
            --exit-status

      - name: Merge the verified snapshot
        if: steps.diff.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.pull_request.outputs.number }}
          SYNC_SHA: ${{ steps.commit.outputs.sha }}
        shell: bash
        run: |
          gh pr merge "$PR_NUMBER" \
            --repo "$GITHUB_REPOSITORY" \
            --squash \
            --subject "YouTube Atomを同期" \
            --match-head-commit "$SYNC_SHA"
